# 🚀 Развёртывание покерного сервера

## Вариант 1: Docker (Рекомендуется)

### Быстрый запуск:

```bash
# Собрать и запустить
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down
```

### Доступ:
- **Локально:** http://localhost:3000
- **В сети:** http://ВАШ_IP:3000 (друзья в той же сети)

---

## Вариант 2: Прямой запуск (без Docker)

```bash
# Установить зависимости
npm install

# Запустить сервер
npm start
```

---

## 🌐 Как поделиться с друзьями из другой сети?

### Способ 1: Cloudflare Tunnel (Бесплатно, без регистрации)

1. Скачайте `cloudflared` с https://github.com/cloudflare/cloudflared/releases
2. Запустите:
   ```bash
   cloudflared tunnel --url http://localhost:3000
   ```
3. Скопируйте URL (например: `https://xxx.trycloudflare.com`)
4. Отправьте друзьям этот URL

### Способ 2: ngrok (Бесплатно, нужна регистрация)

1. Зарегистрируйтесь на https://ngrok.com
2. Установите ngrok
3. Запустите:
   ```bash
   ngrok http 3000
   ```
4. Скопируйте HTTPS URL и отправьте друзьям

### Способ 3: VPS/Хостинг

**Рекомендуемые платформы:**
- **Railway.app** (бесплатный тариф, простой деплой)
- **Render.com** (бесплатный тариф)
- **DigitalOcean** ($5/месяц)
- **Hetzner** (€4/месяц)

---

## 📋 Чеклист для тестирования с друзьями:

- [ ] Сервер запущен (Docker или напрямую)
- [ ] Порт 3000 открыт в firewall (если в одной сети)
- [ ] Получен публичный URL (если в разных сетях)
- [ ] Проверено подключение с другого устройства
- [ ] Создан тестовый стол
- [ ] Добавлены боты/тестовые игроки
- [ ] Игра работает корректно

---

## 🔧 Troubleshooting

**Проблема:** Друзья не могут подключиться
- Проверьте, что сервер слушает на `0.0.0.0` (уже настроено)
- Проверьте firewall на порт 3000
- Используйте туннель (Cloudflare/ngrok) для разных сетей

**Проблема:** Docker не запускается
- Убедитесь, что Docker Desktop запущен
- Проверьте: `docker --version`
- Попробуйте: `docker-compose build --no-cache`

**Проблема:** Порт занят
- Измените порт в `docker-compose.yml`: `"8080:3000"`
- Или остановите процесс на порту 3000
