# 🚀 Деплой покерного сервера

## Вариант 1: Docker (локально или на сервере)

### Локальный запуск через Docker:

```bash
# Сборка и запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f
```

### На облачном сервере (VPS):

1. Загрузите файлы на сервер (через git, scp, или FTP)
2. Установите Docker и Docker Compose
3. Запустите: `docker-compose up -d`
4. Откройте порт 3000 в файрволе сервера

---

## Вариант 2: Бесплатные облачные платформы

### Railway.app (рекомендуется - очень просто!)

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий (или загрузите файлы)
4. Railway автоматически определит Node.js проект
5. Добавьте переменную окружения: `PORT=3000` (обычно не требуется)
6. Деплой произойдет автоматически
7. Railway даст вам публичный URL типа: `https://your-app.railway.app`

**Плюсы:** Бесплатно, автоматический HTTPS, очень просто

---

### Render.com

1. Зарегистрируйтесь на https://render.com
2. Создайте новый "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Environment: `Node`
5. Render даст вам URL типа: `https://your-app.onrender.com`

**Плюсы:** Бесплатно, автоматический HTTPS

---

### Heroku

1. Установите Heroku CLI
2. Создайте файл `Procfile`:
   ```
   web: node server.js
   ```
3. Выполните:
   ```bash
   heroku create your-app-name
   git push heroku main
   ```

---

### Fly.io

1. Установите flyctl
2. Выполните:
   ```bash
   fly launch
   fly deploy
   ```

---

## Вариант 3: VPS сервер (DigitalOcean, AWS, Hetzner и т.д.)

### Быстрый старт:

1. Создайте VPS (Ubuntu 20.04+)
2. Подключитесь по SSH
3. Установите Node.js:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```
4. Установите PM2 для управления процессом:
   ```bash
   sudo npm install -g pm2
   ```
5. Загрузите файлы проекта
6. Установите зависимости:
   ```bash
   npm install --production
   ```
7. Запустите через PM2:
   ```bash
   pm2 start server.js --name poker-server
   pm2 save
   pm2 startup
   ```
8. Откройте порт 3000:
   ```bash
   sudo ufw allow 3000/tcp
   ```

### С Nginx (рекомендуется для продакшена):

1. Установите Nginx:
   ```bash
   sudo apt install nginx
   ```

2. Создайте конфиг `/etc/nginx/sites-available/poker`:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

3. Активируйте:
   ```bash
   sudo ln -s /etc/nginx/sites-available/poker /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl reload nginx
   ```

---

## 🔒 Настройка HTTPS (SSL)

### Для Railway/Render:
Автоматически включен!

### Для VPS с Nginx (Let's Encrypt):

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## 📝 Важные замечания:

1. **Безопасность:** Для продакшена добавьте:
   - Ограничение по IP (если нужно)
   - Rate limiting
   - Аутентификацию (опционально)

2. **Переменные окружения:** Создайте `.env` файл для секретов

3. **Мониторинг:** Используйте PM2 или встроенные инструменты платформы

4. **Бэкапы:** Настройте регулярные бэкапы базы данных (если будет)

---

## 🎮 После деплоя:

Друзья смогут подключиться по вашему публичному URL!

Пример: `https://poker-game.railway.app` или `http://your-server-ip:3000`

