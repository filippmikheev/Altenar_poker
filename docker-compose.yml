services:
  poker-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - poker-network

networks:
  poker-network:
    driver: bridge

