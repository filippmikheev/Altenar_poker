# 🚀 Быстрый запуск через Docker

## Шаг 1: Запустите Docker Desktop

Откройте приложение **Docker Desktop** на вашем компьютере и дождитесь, пока оно полностью запустится (иконка в трее станет зеленой).

## Шаг 2: Запустите покер-сервер

Откройте терминал в папке проекта и выполните:

```bash
docker-compose up -d
```

## Шаг 3: Проверьте статус

```bash
docker ps
```

Должен быть запущен контейнер `poker2-poker-server`.

## Шаг 4: Откройте в браузере

**Локально:** http://localhost:3000

**Для друзей в вашей сети:** http://192.168.1.65:3000

---

## Полезные команды:

```bash
# Остановить сервер
docker-compose down

# Посмотреть логи
docker-compose logs -f

# Перезапустить
docker-compose restart

# Пересобрать и запустить заново
docker-compose up -d --build
```

---

## Если Docker Desktop не установлен:

1. Скачайте: https://www.docker.com/products/docker-desktop
2. Установите и перезагрузите компьютер
3. Запустите Docker Desktop
4. Повторите шаги выше

